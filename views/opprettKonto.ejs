<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Opprett konto</title>
<<<<<<< HEAD
  
  <link rel="stylesheet" href="/styles/opprettKonto.css">
</head>
<body>

  <%- include ('partials/menyPanel') %>
  
  <main class="main-content">
  <div>
    <h1>Opprett konto </h1>
    <form > 
      <input type="text" placeholder="Navnet på din konto" id="kontoNavn">
      <br>
      <input type="text" placeholder="Opprettelsesdato" id="opprettelsesdatoK">
      <br>
      <input type="text" placeholder="Bank" id="bank">
      <br>
      <input type="text" placeholder="Startsaldo" id="saldo">
      <br>
      <button type="button" class="Submit" id="submitbutton">Submit</button>
    </form>
  </div>
</main>

<script>
  const submitbutton = document.getElementById('submitbutton');
  
  const brukernavn = localStorage.getItem('brukernavn');
  
  submitbutton.addEventListener('click', function(event) {
    event.preventDefault(); 

    const kontoNavn = document.getElementById('kontoNavn').value;
    const opprettelsedatoK = document.getElementById('opprettelsedatoK').value;
    const lukkedatoK = document.getElementById('lukkedatoK').value;
    const valuta = document.getElementById('valuta').value;
    const bank = document.getElementById('bank').value;
    const saldo = document.getElementById('saldo').value;

    // console.log(brukernavn, kontoNavn, opprettelsesdatoK, bank, saldo, lukkedatoK, valuta);
   
    fetch('/opprettKonto', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',

      },
      body: JSON.stringify({
        kontoNavn: kontoNavn,
        opprettelsedatoK: opprettelsedatoK,
        bank: bank, 
        saldo: saldo,
        brukernavn: brukernavn,
        lukkedatoK: lukkedatoK,
        valuta: valuta
      })
    })
    .then(async res => {
  const data = await res.json();
  if (!res.ok) throw new Error(data.message);
  alert("Konto opprettet!");
})
.catch(err => {
  console.error(err);
  alert(err.message);
  });
});
</script>
</body>
</html>